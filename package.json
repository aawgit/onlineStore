{
  "name": "nodejs-creating-restful-apis",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "npm install @babel/core  @babel/node @babel/preset-env && cd client && npm install --only=dev && npm install && npm run build",
    "build": "babel server/src -d server/dist",
    "start": "babel-node server/src/server.js",
    "dev": "concurrently \"npm run start\" \"cd client && npm run start\"",
    "restart": "rimraf dist && npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.2",
    "cloudinary": "^1.13.2",
    "concurrently": "^4.1.0",
    "cors": "^2.8.5",
    "dotenv": "^7.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.0",
    "mongoose": "^5.7.5",
    "multer": "^1.4.1",
    "multer-storage-cloudinary": "^2.2.1",
    "nodemailer": "^5.1.1",
    "passport": "^0.4.0",
    "passport-facebook-token": "^3.3.0",
    "randomstring": "^1.1.5",
    "validator": "^11.0.0"
  },
  "engines": {
    "node": "12.19.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.3",
    "@babel/node": "^7.12.1",
    "@babel/preset-env": "^7.12.1"
  }
}
